# Telegram Message Router Bot - نظام توجيه رسائل تلجرام

## 📋 نظرة عامة
نظام متقدم لتوجيه الرسائل من مصادر متعددة إلى أهداف متعددة عبر Telegram/Pyrogram مع دعم:
- جميع أنواع الرسائل والوسائط
- تنسيقات متقدمة (غامق، اقتباس، منشوه، premium emojis)
- لوحة تحكم إبداعية بـ Animations
- قاعدة بيانات PostgreSQL

## 🏗️ البنية المعمارية

```
├── server/
│   ├── index.ts           - نقطة دخول التطبيق
│   ├── db.ts              - اتصال قاعدة البيانات
│   ├── storage.ts         - طبقة تخزين البيانات (CRUD)
│   ├── routes.ts          - مسارات API
│   └── services/          - خدمات منفصلة (قريباً)
│
├── client/
│   └── src/
│       ├── pages/         - صفحات التطبيق
│       ├── hooks/         - React Hooks للعمليات
│       ├── components/    - مكونات React
│       └── lib/           - مكتبات وأدوات مساعدة
│
├── shared/
│   ├── schema.ts          - نماذج قاعدة البيانات
│   └── routes.ts          - تعريفات API
```

## ✅ الميزات المنجزة

### قاعدة البيانات
- ✓ جداول PostgreSQL جاهزة (users, sessions, tasks, logs)
- ✓ علاقات صحيحة بين الجداول
- ✓ أنواع بيانات مرنة (JSONB للفلاتر والخيارات)

### Backend API
- ✓ مسارات CRUD كاملة للجلسات (Sessions)
- ✓ مسارات CRUD كاملة للمهام (Tasks)
- ✓ مسارات Logs لتتبع الرسائل
- ✓ مسار Stats لإحصائيات النظام
- ✓ معالجة أخطاء صحيحة

### Frontend
- ✓ Dashboard بطاقات إحصائيات حية
- ✓ صفحة Tasks مع إنشاء/تعديل/حذف
- ✓ صفحة Sessions لإدارة الحسابات
- ✓ صفحة Logs لتتبع الرسائل
- ✓ نظام تصميم Cyber بـ Animations
- ✓ نظام مصادقة Replit

## 🚀 الميزات القريبة

### معالج توجيه الرسائل
- [ ] تكامل Pyrogram للاتصال بـ Telegram
- [ ] معالج مراقبة المصادر
- [ ] معالج توجيه الرسائل المباشر
- [ ] دعم جميع أنواع الوسائط

### تحسينات الأداء
- [ ] نظام Workers للمعالجة المتوازية
- [ ] تخزين مؤقت (Caching)
- [ ] تحسين استعلامات قاعدة البيانات
- [ ] معالجة تدفق (Streaming) للوسائط الكبيرة

### تحسينات واجهة المستخدم
- [ ] رسوم بيانية متقدمة
- [ ] لوحة تحكم في الوقت الفعلي
- [ ] تنبيهات نظام الوقت الفعلي
- [ ] صفحات إضافية للإعدادات

## 📦 التقنيات المستخدمة

### Backend
- Express.js - خادم ويب
- Drizzle ORM - إدارة قاعدة البيانات
- PostgreSQL - قاعدة البيانات
- TypeScript - لغة البرمجة

### Frontend
- React - مكتبة الواجهات
- TailwindCSS - تنمط CSS
- shadcn/ui - مكونات احترافية
- Framer Motion - الرسوم المتحركة
- Recharts - الرسوم البيانية

## 🔧 كيفية البدء

1. **التثبيت:**
   ```bash
   npm install
   ```

2. **إعداد قاعدة البيانات:**
   ```bash
   npm run db:push
   ```

3. **تشغيل التطبيق:**
   ```bash
   npm run dev
   ```

4. **الدخول:**
   - ادخل عبر Replit Auth
   - اذهب إلى `http://localhost:5000`

## 📝 ملاحظات تطوير

### إضافة جلسة جديدة
```typescript
// في صفحة Sessions، استخدم AddSessionDialog
// يتطلب: sessionName, phoneNumber, sessionString
```

### إنشاء مهمة توجيه جديدة
```typescript
// في صفحة Tasks، استخدم TaskFormDialog
// يتطلب: name, sessionId, sourceChannels, destinationChannels
// اختياري: filters, options
```

### مراقبة السجلات
```typescript
// في صفحة Logs
// يتم التحديث تلقائياً كل 5 ثوانٍ
```

## 🎯 الخطوات التالية

1. بناء معالج Pyrogram للاتصال بـ Telegram
2. تنفيذ نظام المراقبة والتوجيه
3. إضافة نظام Workers للمعالجة المتوازية
4. تحسين الأداء والاستقرار
5. إضافة ميزات متقدمة (spoilers, emojis, etc)

## 📞 الدعم

لأي أسئلة أو مشاكل، يرجى مراجعة الأخطاء في صفحة Logs أو فحص console في الطور التطوير.
